// Визуализация "айсберга" сервера: уровни от "все знают" (верх, над водой)
// до "только старожилы поймут" (низ, глубина). Каждый уровень — список
// фактов/мемов. Читает iceberg_entries (status='published'), сортировка
// по level (int, чем больше — тем глубже).
//
// TODO: export default function IcebergPage()
