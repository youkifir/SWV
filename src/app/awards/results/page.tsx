// Публичная страница результатов. Доступна всем, но отдаёт данные
// только если event_settings.results_published = true (иначе — заглушка
// "результаты объявят после ивента"). Данные берутся из /api/admin/results,
// но без счётчиков голосов по каждому кандидату до публикации.
//
// TODO: export default function ResultsPage()
