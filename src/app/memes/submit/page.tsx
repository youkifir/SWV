// Открытая форма добавления мема: картинка (через тот же ImagePicker/
// /api/admin/upload — TODO решить, разрешать ли загрузку не-админам
// напрямую в Storage или только через POST /api/submissions с URL) + подпись.
// POST /api/submissions с type='meme'.
//
// TODO: export default function MemeSubmitPage()
